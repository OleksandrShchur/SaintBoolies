import React from 'react'

const Header = ({ title }) => {
    return (
      <div class="header">

        <div class='title'>
          ffffffff
        </div>
      </div>
    );
  };

export default Header
